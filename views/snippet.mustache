<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
      <link rel="stylesheet" href="css/styles.css">
    <title>{{title}}</title>
  </head>
  <body>
  <a href="/"> Back </a>
  <h1 style="text-align: center">Code Snippets</h1>

  {{#available}}
    <div style="text-align: center">
    <div>
    <h2> {{title}}</h2>
    </div>
    <div>
    <p style="border: 1px solid black; width: 600px; min-height:300px; word-break: break-all; padding: 15px; margin: auto;">{{body}}</p>
    </div>
    <div>
    <p><span style="font-weight: bold">Snippet Details:</span> {{detail}} </p>
    </div>
    <div>
    <p><span style="font-weight: bold">Language:</span> {{language}} </p>
    </div>
    <div>
    <p><span style="font-weight: bold">Tags:</span> {{tag}} </p>
    </div>
    <form action="/delete/{{id}}" method="post">
    <button type="submit" name="delete"> Delete Snippet </button>
    </form>
    <form action="/edit" method="get">
      <button type="submit" name="edit"> Edit Snippet </button>
    </form>
    </div>
    {{/available}}
  </body>
</html>
